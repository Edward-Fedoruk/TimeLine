const styles = () => ({
  timeBlock: {
    // minHeight: '100vh',
    backgroundColor: 'gray',
    height: 'auto',
    position: 'relative',
    // padding: '30px 0',
    // transform: 'rotate(180deg)',
    transition: 'all 3s',
    width: '100%'
  }
})

export default styles